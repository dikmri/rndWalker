# rndWalker v0.2.6

プリセット操作とテンキー操作を強化した改善リリースです。

## 改善内容

### プリセットの操作性を改善

- プリセットごとに F1〜F4、F6〜F10、F12 を割り当てて即時切替できます。
- 設定画面で選択中のプリセットは、「保存」で現在の設定と同時に上書き保存されます。
- 再起動時も現在のプリセットを復元し、旧設定は現在のフォルダ構成から自動移行します。

### テンキー操作を強化

- テンキー操作を有効にするとフォルダグループを1〜9まで利用できます。
- テンキー1〜9で各フォルダ、0で全フォルダへ切り替えます。
