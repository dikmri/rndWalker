# JS動画ランダム再生プレイヤー

## 2026-01-11 6:51

- 指定のフォルダのmp4とmp3を同時にランダムで再生するシンプルなプレイヤー。
- ひとまずWindows環境で動作するGUIソフトウェアとして作成。将来的にはMacやLinuxでも動作するようにしたい。
- html,css,javascriptとElectronでexeファイルを作ってください。
- F11で全画面モード切替
- 初起動時にmp4のフォルダとmp3のフォルダを指定し、次回以降起動時は自動でそのフォルダを読み込むようになる。もちろん設定で変更可能。
- Mでミュート。↑/↓キーで音量調整。→キーで今再生している動画をスキップし、次のランダムな動画を再生。←キーで今再生している動画をスキップし、前再生していた動画を再生。
- 実装/改修時は/docsフォルダ内に.md形式で設計書を作成、更新し、それを元に実装するようにする。これは永続的な命令です。
- コーディング規約はGoogle標準とする。これは永続的な命令です。
- これは永続的な命令です。私が「いつもの」と指示した場合、それは「memo.md の最新の指示を元に作業を進めてください。」という意味になります。そのように意味を捉えてください。
- これは永続的な命令です。実装/改修した際は必ずgitにコミットするようにしてください。
- これは永続的な命令です。日付などを入れるときはちゃんとコマンドで取得して確認してから入れるようにしてください。

## 2026-01-11 7:01

- 動作確認しましたが、mp3は再生出来ていますが、mp4は再生出来ていないようです。画面が真っ暗のままです。調査と修正お願いします。

## 2026-01-11 7:08

- 動作確認しましたが、相変わらず動画が再生されず画面が真っ暗のままです。また、mp3もどうやらランダム再生というより、ループ再生になっているようですが、そうではなく、1つのファイルが再生終了したら次のランダムなmp3が再生されるという仕組みです。mp4に関しても同様です。というか早くmp4がまともに再生されるようにしてほしいです。

- memo.md はgitでコミットされないようにしておいて。